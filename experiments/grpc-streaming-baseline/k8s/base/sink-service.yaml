apiVersion: v1
kind: Service
metadata:
  name: grpc-stream-sink
  namespace: streaming-experiments
spec:
  selector:
    app: grpc-stream-sink
  ports:
    - name: grpc
      port: 50052
      targetPort: grpc
    - name: metrics
      port: 9102
      targetPort: metrics
