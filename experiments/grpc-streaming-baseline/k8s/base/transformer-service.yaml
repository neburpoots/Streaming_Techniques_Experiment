apiVersion: v1
kind: Service
metadata:
  name: grpc-stream-transformer
  namespace: streaming-experiments
spec:
  selector:
    app: grpc-stream-transformer
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
    - name: metrics
      port: 9101
      targetPort: metrics
