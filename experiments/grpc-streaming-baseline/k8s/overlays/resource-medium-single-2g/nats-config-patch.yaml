apiVersion: v1
kind: ConfigMap
metadata:
  name: grpc-stream-nats-config
  namespace: streaming-experiments
data:
  nats.conf: |
    listen: 0.0.0.0:4222
    max_pending: 512MB

    jetstream {
      store_dir: "/data/jetstream"
      max_mem_store: 512MB
      max_file_store: 40GB
    }
